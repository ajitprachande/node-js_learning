## in this day moving to in memory database to real database application. 
- here I used Drizzle ORM and PostgreSQL.
- created two controllers: `brand controller` and `products controller`
- inside brand controller:-> getBrand, getBrandById, createBrand, getProductsByBrand,
- inside products controller:-> getAllProducts, searchProducts, getProductById, createeProduct, deleteProductByID 

- inside middleware dir: I created logging route history (logger.txt) file.
- 